* add missing basic operators
* add array indexing `[]` operator support
* add `for` loops
* docs: add examples
* more end2end tests
* adjusting frame size for the template
* nested/partial templates
* full double quote strings support
* filesystem bytecode cache
* float literals, values and constants
* builtin functions
* output escaping settings
* macros system
* compile errors for long jumps/too many consts and vars, etc
* `$x - 1` should be parsed OK (currently lexing -1 as negative int)
* refactor compiler code
* don't discard type information in things like compileTempExpr
